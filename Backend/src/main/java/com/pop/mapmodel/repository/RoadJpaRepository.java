package com.pop.mapmodel.repository;

import com.pop.mapmodel.domain.Road;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoadJpaRepository extends JpaRepository<Road, Long> {
}
